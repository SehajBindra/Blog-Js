import React from "react";
import Savepost from "./Savepost";

function Saved({ id, title, img, username, userImage }) {
  return (
    <div>
      <h1>{title}</h1>
    </div>
  );
}

export default Saved;
